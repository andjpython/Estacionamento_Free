services:
  - type: web
    name: estacionamento-rotativo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SENHA_SUPERVISOR
        value: "290479"
